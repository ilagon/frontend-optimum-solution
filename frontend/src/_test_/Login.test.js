//Configuration Test : To check if Jest is Working 

//describe is the test suite
//it it test
// test(enzyme) == it(jest)
//enzyme has shallow rendering and full dom rendering , static rendered markup

describe('Addition', () => { it('knows that 2 and 2 make 4', () => { expect(2 + 2).toBe(4); }); }); 